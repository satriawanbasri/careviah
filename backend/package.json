{
    "scripts": {
        "start": "nodemon -r dotenv/config src",
        "live": "node -r dotenv/config src"
    },
    "dependencies": {
        "dotenv": "^16.0.3",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    }
}
